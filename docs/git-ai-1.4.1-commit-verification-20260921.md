# git-ai 1.4.1 commit 采集验证

## 验证范围

- 日期：2026-09-21；客户端：Codex；git-ai：1.4.1。
- 在 claude-skills 仓库新增本记录，执行一次真实 commit 和 push。
- 不写入模拟 metrics，不伪造 Token，不修改 hooks 配置。
- 保留本仓库配置的人类作者，并添加 Codex 共同作者 trailer。

## 验收方法

1. 记录本次提交的完整 SHA，确认远端分支已包含该提交。
2. 在本地 metrics 中按提交 SHA 查找 e=1，核对仓库和参与者。
3. 检查对应事件的 delivered_ts；已投递不等于平台已经计入。
4. 等平台刷新后，比较执行前后的提交数与行数增量。

## 判据边界

- 本文件是验证输入，不预先声明采集、投递或平台统计成功。
- GitHub 共同作者识别与 CodePulse 采集是两项独立检查。
- 不以 push 成功或进程退出码替代事件投递状态。
- 若存在并发上报，不把平台全部增量归因于本次提交。
- Token 可能随会话结束后更新，不预设本次提交的 Token 增量。
